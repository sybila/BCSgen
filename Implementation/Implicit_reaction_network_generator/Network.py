from Edge import *
from Node import *

class Network:
	def __init__(self):
		self.Nodes = set([])
		self.Edges = set([])
		self.Reactions = set([])

	def addNode(self, header):
		self.Nodes.

	def createNetwork(self, bcsModelFile):
